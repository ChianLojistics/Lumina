title = "Lumina gitleaks config"

[extend]
useDefault = true

[allowlist]
description = "Test fixtures and examples, not real credentials"
paths = [
  '''\.env\.example$''',
  '''.*\.spec\.ts$''',
  '''.*\.test\.ts$''',
]
